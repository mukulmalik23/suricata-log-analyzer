%YAML 1.1
---

default-log-dir: /var/log/suricata

outputs:

  - eve-log:
      enabled: yes
      filename: eve.json
      types:
        - alert
        - http
        - dns

pcap:

  - interface: eth0

default-rule-path: /var/lib/suricata/rules

rule-files:

  - suricata.rules
